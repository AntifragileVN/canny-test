import { Integrations } from 'pages/Integrations';
import { MainContainer } from './Layout.styled';

export const Layout = () => {
	return (
		<MainContainer>
			<Integrations />
		</MainContainer>
	);
};
